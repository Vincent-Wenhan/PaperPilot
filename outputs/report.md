# PaperPilot Reproduction Report

> Example report generated in `LLM_MOCK_MODE=True`.

## Paper Information

示例 PDF：用户上传的任意可提取文本论文 PDF。Mock mode 仅确认流程可以运行。

## Method Overview

真实 LLM 模式下，本节将包含模型结构、模块输入输出、损失函数、训练流程、推理流程和复现难点。

## Code Repository

- Example URL: `https://github.com/octocat/Hello-World`
- Clone mode: `git clone --depth 1`
- Local location: `workspace/<owner>__<repository>`

## Environment

- Example hardware: CPU only
- Recommended project environment: Conda `paperpilot`, Python 3.12

## Data Preparation

请依据论文与仓库 README 手动准备数据。PaperPilot 不会默认下载大型数据集、模型权重或 checkpoint。

## Commands

默认仅建议 `python --version`、`pip --version` 和候选入口的 `--help`。完整 demo 与训练必须由用户确认。

## Debug Notes

Runner 失败时会保留 command、return code、stdout 和 stderr，并自动调用 Debug Agent。用户也可以手动粘贴日志。

## Difference from Original Paper

本示例用于验证课程项目流程，不代表已经复现原论文实验设置、资源规模或指标。

## Next Steps

1. 上传目标论文 PDF。
2. 提供对应公开 GitHub 仓库。
3. 检查 Agent 生成的环境与实验计划。
4. 先运行 version 和 `--help`。
5. 用户确认后再尝试 demo 或最小训练实验。

