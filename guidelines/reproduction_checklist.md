# Reproduction Checklist

Identify task, method modules, data, metrics, training and inference settings,
repository entry points, dependencies, checkpoints, hardware assumptions,
minimal runnable path, risks, missing information, and fallback options.
