# Streamlit UI Rules

Use an explicit input, one primary action, understandable progress, structured
results, limitations, and download support. Show capability cards, composition,
PRD/MVP, prototype plan, and evaluation separately. Never launch generated
products or execute analyzed repositories automatically.
